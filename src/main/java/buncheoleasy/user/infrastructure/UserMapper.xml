<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="buncheoleasy.user.infrastructure.UserMapper">

    <select id="findById" resultType="User">
        SELECT *
        FROM users
        WHERE id = #{id}
          AND deleted_at IS NULL
    </select>

    <select id="findBySocialId" resultType="User">
        SELECT *
        FROM users
        WHERE social_id = #{socialId}
          AND deleted_at IS NULL
    </select>

    <insert id="save" parameterType="User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (social_id, nickname, email)
            VALUES (#{socialId}, #{nickname}, #{email})
    </insert>

    <update id="updateNickname">
        UPDATE users SET nickname = #{nickname}
                     WHERE id = #{id}
                       AND deleted_at IS NULL
    </update>

    <update id="updatePhoneNumber">
        UPDATE users SET phone_number = #{phoneNumber}
                     WHERE id = #{id}
                       AND deleted_at IS NULL
    </update>

    <update id="softDelete">
        UPDATE users SET deleted_at = #{deletedAt}
                     WHERE id = #{id}
                       AND deleted_at IS NULL
    </update>

</mapper>
